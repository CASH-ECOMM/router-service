# Example .env file for local development
# Copy to .env and customize as needed

# Scenario 1: Router local + all services in Docker
# Run => ./mvnw spring-boot:run -Dspring.profiles.active=local
# Or source this file => source .env.local

# Scenario 2: Router local + some services local + rest in Docker
# Override individual services:
USER_SERVICE_ADDRESS=static://localhost:50051
CATALOGUE_SERVICE_ADDRESS=static://localhost:50052
PAYMENT_SERVICE_ADDRESS=static://localhost:50053
AUCTION_SERVICE_ADDRESS=static://localhost:50054

# Example: If user-service runs locally on port 1234 and rest in Docker
# USER_SERVICE_ADDRESS=static://localhost:1234
# CATALOGUE_SERVICE_ADDRESS=static://localhost:50052
# PAYMENT_SERVICE_ADDRESS=static://localhost:50053
# AUCTION_SERVICE_ADDRESS=static://localhost:50054
